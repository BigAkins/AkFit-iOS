// Debug build configuration
// Values are loaded from Secrets.xcconfig, which is gitignored.
// Copy Config/Secrets.xcconfig.template → Config/Secrets.xcconfig and fill in your values.
#include? "Secrets.xcconfig"
